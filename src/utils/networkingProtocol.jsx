// networking protocol
// happens at the end of each turn
 // could use event listeners on each persons end. when it their turn they go

export const networkingProtocol = () => {
        // maybe it is two functions
        // first do the socket.io bit to get that working

        /// after gamestate is set
        /// may reset order
        /// change out of turn
        /// new user is in turn 
        /// send push new gamestate to each other user
        /// this is how the new person knows its their turn
};
